version: "2"

linters:
  default: standard
  settings:
    staticcheck:
      checks:
        - all
        # ST1000 (パッケージコメント必須) はテスト用パッケージで冗長なため除外
        - "-ST1000"

formatters:
  enable:
    - gofmt
